server {
    listen 80;
    server_name spring-backend-lab.ru auth.spring-backend-lab.ru;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    http2 on;
    server_name spring-backend-lab.ru;

    ssl_certificate /etc/letsencrypt/live/spring-backend-lab.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/spring-backend-lab.ru/privkey.pem;

    location / {
        proxy_pass http://bff:8082;

        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;

        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port 443;
    }
}

server {
    listen 443 ssl;
    http2 on;
    server_name auth.spring-backend-lab.ru;

    ssl_certificate /etc/letsencrypt/live/spring-backend-lab.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/spring-backend-lab.ru/privkey.pem;

    location / {
        proxy_pass http://keycloak:8080;

        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;

        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port 443;
    }
}